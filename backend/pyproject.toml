[project]
name = "zasnet-broadcast-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi", "uvicorn[standard]", "pydantic", "pydantic-settings", "python-multipart", "aiofiles", "MetPy>=1.7,<2"]
[tool.pytest.ini_options]
pythonpath = ["."]
