import { createScene, createSceneObject, newId, type Scene, type SceneObject, type SceneObjectType } from './types';
export function addObject(scene: Scene, type: SceneObjectType) { const object = createSceneObject(type); return { ...scene, objects: [...scene.objects, object], modifiedAt: new Date().toISOString() }; }
export function updateObject(scene: Scene, id: string, patch: Partial<SceneObject> & { properties?: Record<string, unknown> }) { return { ...scene, objects: scene.objects.map(object => object.id === id ? { ...object, ...patch, properties: patch.properties ? { ...object.properties, ...patch.properties } : object.properties } : object), modifiedAt: new Date().toISOString() }; }
export function removeObject(scene: Scene, id: string) { return { ...scene, objects: scene.objects.filter(object => object.id !== id), modifiedAt: new Date().toISOString() }; }
export function reorderObject(scene: Scene, id: string, direction: -1 | 1) { const ordered = [...scene.objects].sort((a, b) => a.z - b.z), index = ordered.findIndex(object => object.id === id), target = index + direction; if (index < 0 || target < 0 || target >= ordered.length) return scene; [ordered[index], ordered[target]] = [ordered[target], ordered[index]]; return { ...scene, objects: ordered.map((object, i) => ({ ...object, z: i })), modifiedAt: new Date().toISOString() }; }
export function duplicateScene(scene: Scene): Scene { const now = new Date().toISOString(); return { ...scene, id: newId('scene'), name: `${scene.name} Copy`, objects: scene.objects.map(object => ({ ...object, id: newId('object') })), createdAt: now, modifiedAt: now }; }
export { createScene };
